"use client";
import type { Tab } from "@/stores/tabs-store";

/**
 * Placeholder — full implementation lands in Phase 3-B Task 7
 * (react-pdf against /api/notes/:id/file).
 */
export function SourceViewer({ tab: _tab }: { tab: Tab }) {
  return <div data-testid="source-viewer-placeholder" />;
}
