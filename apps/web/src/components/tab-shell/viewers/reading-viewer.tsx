"use client";
import type { Tab } from "@/stores/tabs-store";

/**
 * Placeholder — full implementation lands in Phase 3-B Task 6
 * (readOnly Plate + font slider via useCollaborativeEditor).
 */
export function ReadingViewer({ tab: _tab }: { tab: Tab }) {
  return <div data-testid="reading-viewer-placeholder" />;
}
