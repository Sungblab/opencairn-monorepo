"use client";
import type { Tab } from "@/stores/tabs-store";

/**
 * Placeholder — full implementation lands in Phase 3-B Task 8
 * (react-json-view-lite against /api/notes/:id/data).
 */
export function DataViewer({ tab: _tab }: { tab: Tab }) {
  return <div data-testid="data-viewer-placeholder" />;
}
