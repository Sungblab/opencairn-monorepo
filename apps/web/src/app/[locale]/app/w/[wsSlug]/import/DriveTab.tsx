"use client";

// Placeholder shell — Task 15 fills in Google Picker + file list + start button.
// Kept minimal so the page can render while Task 15 lands.
export function DriveTab({ wsSlug: _wsSlug }: { wsSlug: string }) {
  return (
    <div className="rounded border border-dashed border-border p-6 text-sm text-muted-foreground">
      Drive tab — Task 15
    </div>
  );
}
