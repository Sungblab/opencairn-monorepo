"use client";

// Placeholder shell — Task 16 fills in ZIP upload + progress + start button.
// Kept minimal so the page can render while Task 16 lands.
export function NotionTab({ wsSlug: _wsSlug }: { wsSlug: string }) {
  return (
    <div className="rounded border border-dashed border-border p-6 text-sm text-muted-foreground">
      Notion tab — Task 16
    </div>
  );
}
