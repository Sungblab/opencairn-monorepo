"use client";

// Ctrl/Cmd+Shift+R is the browser hard-refresh shortcut. Do not bind it to
// reading mode; users can switch note modes explicitly from the tab menu.
export function useTabModeShortcut() {
  return;
}
