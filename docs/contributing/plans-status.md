# Plans Status

Critical path (Phase 0 → 1 → 2 → 3 순서. Phase 0 내부는 **직렬** (`Plan 1 → Plan 13 → Plan 12`). Phase 1은 Plan 12 완료 후 **Plan 2/3/4/9 병렬**, Phase 2는 Plan 4 완료 후 **Plan 5/6/7/8 병렬**.).

Plan 파일 위치: `docs/superpowers/plans/`.

## Phase 0 — Foundation (직렬) ✅

| Plan                                 | Status                            | Summary                                                                                                                                                                                                      |
| ------------------------------------ | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `2026-04-09-plan-1-foundation.md`    | ✅ 2026-04-20, HEAD `50eaf3b`    | Monorepo, DB schema (Workspace 3계층 + 권한), Better Auth, workspace/member/invite CRUD, permissions helpers, Docker, Resend, CI/CD. Task B1 (백업 스크립트)은 배포 준비 단계에서 별도.                |
| `2026-04-13-multi-llm-provider.md`   | ✅ 2026-04-20                     | `packages/llm` (Gemini/Ollama async providers + factory, 22/22 pytest), `user_preferences` 테이블, Ollama docker profile, LLM env block.                                                                     |
| `2026-04-20-plan-12-agent-runtime.md` | ✅ 2026-04-20                     | `apps/worker/src/runtime/` facade (60/60 pytest): `@tool`, `AgentEvent` 9종 + Zod mirror, `Agent` ABC + LangGraph stream adapter, 3계층 훅, NDJSON trajectory + `agent_runs` 테이블, default hooks, eval. |

## Phase 1 — Core

| Plan                                                   | Status                            | Summary                                                                                                                                                                                                                                                                                                              |
| ------------------------------------------------------ | --------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `2026-04-21-plan-2a-editor-core.md`                    | ✅ 2026-04-21, HEAD `aa49113`   | Plate v49 기반 솔로 에디터: basic nodes + LaTeX(KaTeX) + wiki-link(Cmd+K combobox) + slash menu(9 commands) + debounced save/load + 사이드바(folder tree + note list). Hocuspocus/코멘트/@mention 없음. 기존 Plan 2 Task 1~7 범위 대체. |
| `2026-04-22-plan-2b-hocuspocus-collab.md`              | ✅ 2026-04-22, merge `f212413`   | `apps/hocuspocus` Yjs 협업 서버 (Better Auth 세션 + `resolveRole` → readOnly, `@slate-yjs/core` plate-bridge, `@hocuspocus/extension-database` 영속화, readonly-guard, block-orphan-reaper), migration 0010 (`comments` + `comment_mentions` + `yjs_documents`) + 0011 (commenter role), `/api/notes/:noteId/comments` CRUD + `/api/comments/:id/resolve`, `/api/mentions/search` (user/page/concept), `/api/notes/:id/role`, 웹 NoteEditor Yjs 마이그레이션 (PATCH content 경로 삭제 — Yjs canonical) + Presence/Readonly/Disconnected 배너 + CommentsPanel + `@` 자동완성. Playwright 3-브라우저 E2E. api 44 + hocuspocus 37 + db 4 + shared 5 = 90 tests. 기존 Plan 2 Task 8~11 범위 대체. |
| `2026-04-26-plan-2c-share-notifications.md`            | ✅ 2026-04-26 (`feat/plan-2c-share-notifications`) | Plan 2C — 공개 공유 링크 + per-note 권한 + 누락된 3 종 알림 wiring(`comment_reply`/`share_invite`/`research_complete`). 12 task / 13 commits. **DB**: migration 0026 `share_links` (token unique + `WHERE revoked_at IS NULL` partial active-token index, soft-revoke). **API**: `routes/share.ts` (POST/GET/DELETE `/notes/:id/share` idempotent-by-role + `GET /workspaces/:wsId/share` admin + `GET /public/share/:token` no-auth + 30 req/min/IP rate limit + Y.Doc → Plate value SSR 디코딩) + per-note permissions(POST/GET/PATCH/DELETE `/notes/:id/permissions` + `share_invite` 알림 발화 self-skip & no-op skip) + `GET /workspaces/:workspaceId/members/search` ILIKE · `comments.ts` `comment_reply` fan-out (silent-on-failure) · `internal.ts` `PATCH /research/runs/:id/finalize` (tx + FOR UPDATE + previouslyCompleted idempotency, `research_complete` 알림 only on first completed). **Worker**: `activities/deep_research/finalize.py` + workflow 5 terminal paths(success/failed/3 cancelled) + `RetryPolicy(max=5)`. **Web**: `/[locale]/s/[token]` SSR + hand-rolled `PlateStaticRenderer` (Plate v49 미노출, `noindex`/`no-referrer` meta) + `ShareDialog`(invite + web sections, shadcn Dialog) + 노트 헤더 Share 버튼(`!readOnly` 게이트) + `SharedLinksTab` 채우기(workspace-wide list + revoke) + `NotificationItem` 4-kind 분기 + drawer click routing(`/${locale}/app/w/${wsSlug}/n/${noteId}` 패턴 재사용). **i18n**: `public-share` + `share-dialog` 신규 namespace + `notifications.summary.*` + `workspace-settings.sharedLinks.*` (모두 ko/en parity, snake_case kindLabels 유지). api 460/460 · web 451/451 · worker test_finalize 3/3 · tsc clean(`code.ts:147` 은 Plan 7 P2 pre-existing) · i18n parity 24/24. **Hono mount-order finding**: 동일 `/api` prefix 다중 sub-app + `.use("*", requireAuth)` 가 다른 sub-app 의 public 라우트를 가로채는 문제 → 본 plan은 per-route `requireAuth` 부착으로 해결. **Out of scope (별도 plan)**: 이메일 알림(Plan 2 Task 14), 비밀번호/만료/SEO 토글, `system` 알림 발송(Super Admin Console), 공유 페이지 `editor` role 활성화(Hocuspocus 비인증 read 확장), Playwright E2E. |
| `2026-04-26-plan-2d-chat-renderer.md`                  | ✅ 2026-04-27 PR #52 MERGED (main `d766cd0`), 28 commits   | Plan 2D — Chat Renderer + 5 Editor Blocks + save_suggestion flow. Phase A foundation (deps install + i18n keys + `saveSuggestionSchema` zod + `activeEditorStore` zustand). Phase B markdown utility (`markdownToPlate` GFM/mermaid/callout post-processors + `shared-prose` Tailwind tokens + `sanitizeHtml` DOMPurify wrapper). Phase C 5 Plate v49 blocks (Mermaid void + lazy `useMermaidRender` + Callout 4-kind cycle + Toggle collapsible summary/body + `@platejs/table` registration + `@platejs/layout` columns w/ `column_group` underscore key). Phase D editor integration (slash menu +5 commands + ` ```mermaid ` fence autoformat). Phase E chat renderer (react-markdown + remark-gfm/math + rehype-katex/raw + Prism `CodeBlock` w/ copy + `MermaidChat` lazy + `CalloutBlockquote` `[!kind]` detection + `StreamingCursor` + wired into MessageBubble). Phase F save_suggestion flow (NoteEditor registers w/ activeEditorStore + `insertFromMarkdown`/`createNoteFromMarkdown` helpers + AgentPanel `handleSaveSuggestion` w/ projectId resolver + sonner toast w/ create-new fallback + apps/api stub emits SSE chunk under `AGENT_STUB_EMIT_SAVE_SUGGESTION=1` + `/test-save` marker). Phase G E2E (3 specs · 12 tests · `--list` clean, run deferred to CI). web 484/484 vitest · tsc clean (Plan 2D 도입 TS errors `9d8906b`에서 fix) · ESLint clean (단, Plan 5 KG Phase 2 잔존 lint error 1건 미해결 — Plan 2D 무관). Plate v49 패턴: `createPlatePlugin().withComponent()`, void 엘리먼트 children render via display:none span, `Omit<PlateElementProps,'element'> & { element: ... & { kind } }` 인터페이스 패턴. Out of scope (Plan 2E로 이연): Plan 11B 본체(provenance/slash 6/related pages), Image/Embed, drag-resize column, table 행/열 컨텍스트 메뉴, math 입력 UX, mermaid theme reactivity, escape-norm cleanup. |
| `2026-04-09-plan-2-editor.md` (Task 12~21)             | 🟡 2C/2D 완료 — 잔여는 2E + 이메일 dispatcher          | 2C(공유 링크 + per-note 권한 + 알림 wiring 3종) + 2D(chat 렌더러 + 5 에디터 블록 + save_suggestion) 완료. 잔여: 이메일 알림 dispatcher(Plan 2 Task 14, 별도 plan). **Multi-Mode Tab Shell + Split Pane + Diff View + Command Palette**는 App Shell Phase 1~5 (`2026-04-23-app-shell-phase-*.md`)로 대체. **Plan 2E(이연)**: Plan 11B 본체(provenance/slash 6/related pages), Image/Embed, drag-resize column, table 행/열 컨텍스트 메뉴, math 입력 UX, mermaid theme reactivity, escape-norm cleanup. 상세 specs: `tab-system-design.md`. |
| `2026-04-23-app-shell-phase-1-shell-frame.md`          | ✅ 2026-04-23 (HEAD: 본 커밋)     | App Shell Phase 1 — 빈 3-panel shell. `users.last_viewed_workspace_id` + PATCH/GET endpoints, jsdom vitest 프로젝트, `useBreakpoint`/`useKeyboardShortcut`/`useUrlTabSync` 훅, 5 Zustand 스토어 (panel/tabs/threads/sidebar/palette), `tab-url` ↔ URL 헬퍼, `AppShell` 3-panel + resize + Sheet overlay (<lg), `(shell)` 라우트 그룹 + 4 placeholder 페이지 (`n/[noteId]`, `research`, `research/[runId]`, `settings/[[...slug]]`), `/app` last-viewed 우선 redirect, `/{locale}` proxy 보강. `app-shell.json` i18n (14 keys). web 83/83 + api 169/169 unit + 6/6 Playwright. Phase 2~5는 후속 plan. |
| `2026-04-23-app-shell-phase-2-sidebar.md`              | ✅ 2026-04-23, HEAD `43c751a`     | App Shell Phase 2 — Sidebar internals. Task 1~7(백엔드): ADR 009 ltree-on-folders, migration 0018 `folders.path` + GIST, `tree-queries.ts`(listChildren/move/subtree) + 16 tests, `/api/projects/:id/tree` + `/permissions` + `/stream/projects/:id/tree` SSE, `useProjectTree` React Query + SSE invalidation. Task 8~16(UI): `GET /api/workspaces/me`(workspaces+pending invites), WorkspaceSwitcher + GlobalNav + MoreMenu + ProjectHero + ProjectSwitcher + SidebarFooter + ScopedSearch, `react-arborist` + `@dnd-kit` 기반 virtualized ProjectTree(ResizeObserver 기반 auto-height), PATCH /api/folders/:id 의 `parentId` 변경 → `moveFolder` ltree 재작성, PATCH /api/notes/:id/move 신설 + `content`/`folderId` 를 `PATCH /:id` 에서 strip (cross-project 누수 방지), 인라인 rename(Enter/Escape/blur + skipBlur 가드) + 우클릭 context menu(rename/delete, duplicate/copy-link stub), F2·⌘Del·type-ahead 키보드, `ShellSidebar` 조립 + `AppShell` 교체, `sidebar.json` 41 keys(ko/en parity), `tests/e2e/sidebar.spec.ts` + `seed-5k-nodes.ts` (실행 deferred). api 23 files/217 tests · web 24 files/123 tests · tsc clean · ESLint clean. Follow-ups (feat/app-shell-phase-2-followups에서 a/c/d/e 해결): ~~(a) folders PATCH의 same-parent position-only SSE 미발행~~ `tree.folder_reordered` 이벤트 추가 + no-op 재전송 시 silent, (b) notes는 `position` 컬럼 없음 → 재정렬 UX lie, Phase 3/2C에 위임, ~~(c) `ProjectTree.onMove` 토스트 부재~~ sonner 레이어 + i18n 연결, ~~(d) placeholder-sidebar.tsx 삭제~~, ~~(e) `/api/internal/test-seed-bulk` 엔드포인트~~ 구현 + 5 tests. 잔여: (b) + perf `@perf` 스펙 실제 실행 + Toaster가 `(shell)` 그룹 전용이라 `/onboarding` · `/auth`에서는 안 뜸. |
| `2026-04-23-app-shell-phase-5-routes-palette.md`       | ✅ 2026-04-26 (`feat/app-shell-phase-5`) | App Shell Phase 5 — 남은 라우트(대시보드/프로젝트/워크스페이스 settings) + 계정 settings shell(`/[locale]/settings/*`, outside AppShell) + Command Palette(cmdk, ⌘K) + Notifications(API + DB + SSE + drawer). Plan Task 3·4·5(research hub/run/import wizard) 는 Phase D + Ingest Expansion 으로 이미 충족 → SKIP. 신규 endpoints: `/workspaces/:id/{stats,recent-notes,notes/search}`, `/workspaces/:id/invites` GET/DELETE, `/projects/:id/notes?filter=`, `/users/me` GET/PATCH, `/notifications` GET + `:id/read` PATCH, `/stream/notifications` SSE. `notification-events.ts` event bus + `persistAndPublish` helper, comments POST 가 user mention 시 publish (다른 사이트 follow-up). DB migration 0024 (`notifications` + `notification_kind` enum + partial unread index). Web: `(shell)/page.tsx`/`p/[projectId]/page.tsx`/`settings/[[...slug]]/page.tsx` 모두 placeholder → real view 교체, `(shell)` 밖 옛 `p/[projectId]/page.tsx` 삭제(URL 충돌 회피, layout/notes/[noteId]는 유지). Toaster 가 `[locale]/layout.tsx` 로 lift (Phase 2 잔여 follow-up 해결). 5 신규 i18n namespace (`project`/`workspace-settings`/`account`/`palette`/`notifications`) + `i18n.ts` 등록, dashboard.json 22 키 신규. api 339/339 · web 335/335 · tsc clean · ESLint clean (max-warnings 0) · 모든 namespace ko/en parity. `tests/e2e/{palette,notifications,routes}.spec.ts` 작성 · 실행 deferred (Phase 4 관례). Follow-ups: (a) publish wiring 확장(comment_reply / research_complete / share_invite / system), (b) 응답 snake/camel 일관화, (c) `/api/notifications` 페이지네이션, (d) `users.locale`/`timezone` 컬럼 + form binding, (e) `/api/search/text` Postgres FTS 확장(현재 title ILIKE), (f) palette 가 (shell) 안에서만 — 외부 라우트(/settings 등)에서도 활성화, (g) E2E 실행. |
| `2026-04-23-app-shell-phase-4-agent-panel.md`          | ✅ 2026-04-25, HEAD `b1c13d3` (`feat/app-shell-phase-4-agent-panel`) | App Shell Phase 4 — Agent Panel. Real chat UI replaces Phase 1 placeholder. Migration `0020_chat_redesign` (chat_threads + chat_messages + message_feedback + message_role/message_status enums, workspace+user 스코프, message_feedback unique on (message_id,user_id) + sentiment CHECK). Hono 라우트: `GET/POST/PATCH/DELETE /api/threads`, `GET /api/threads/:id/messages`, `POST /api/threads/:id/messages` (SSE: `user_persisted` → `agent_placeholder` → `status` → `thought` → `text*` → `citation*` → `save_suggestion?` → `done`; 실패 시 `error` + agent row `failed` + partial buffer), `GET/POST /api/message-feedback` (upsert). Stub `runAgent` in `apps/api/src/lib/agent-pipeline.ts` (deterministic placeholder; 실제 LLM 배선은 follow-up). Web: typed `chatApi` + `useChatThreads`/`useChatMessages`/`useChatSend` hooks, `agent-panel/*` 컴포넌트 (thread list + 모드 selector + scope chips + thought bubble + status line + citation chips + save-suggestion + thumbs feedback wrappers), full panel assembly swap. `agent-panel.json` 44 keys (ko=en parity). api 277/277 · web 244/244 · lint clean (max-warnings 0) · tsc clean (api+web). E2E spec `tests/e2e/agent-panel.spec.ts` 커밋되었으나 실행 deferred (sibling worktree dev-server collision; Onboarding 관례). Follow-ups: 실제 agent pipeline 배선 (humanizer + model-router spec 통합), Plan 11A/11B citations + save-suggestion producer, voice mic 기능, cross-workspace thread share. |
| `2026-04-25-app-shell-phase-3b-tab-mode-router-and-viewers.md` | ✅ 2026-04-25, merge `83f5076`    | App Shell Phase 3-B — TabModeRouter + core viewers. `Tab.titleKey` + `useResolvedTabTitle` (next-intl `t.has()` 체크, ko/en locale lock-in 해소). `NoteEditor.onFirstEdit` paste/drop 확장 (one-shot + readOnly 게이트 유지). `GET /api/notes/:id/file` (MinIO stream · RFC 6266 dual filename · Node→Web ReadableStream back-pressure) + `GET /api/notes/:id/data` (contentText JSON parse → `{ data \| null }`, 403 missing/404 no-key). `TabModeRouter` + `isRoutedByTabModeRouter` predicate + `StubViewer` fallback + `ReadingViewer` (readOnly Plate + font slider 14–22px, `useCollaborativeEditor` 재사용으로 live-sync) + `SourceViewer` (`react-pdf` + `pdfjs-dist` worker via `new URL(import.meta.url)`) + `DataViewer` (`react-json-view-lite`). `TabShell` 분기 (plate → children, 그 외 → TabModeRouter). `⌘⇧R` plate↔reading 토글 + tab context-menu "모드 변경" submenu (Base UI `SubmenuRoot`/`RadioGroup` 래퍼 5개 추가). `appShell.viewers.*` + `appShell.tabs.menu.mode.*` i18n (ko/en parity 43 keys). web 40 files/229 tests · api 26 files/234 tests · tsc clean · ESLint clean. `tests/e2e/tab-viewers.spec.ts` 작성·실행 deferred (Phase 3-A 관례 + `/test-seed` source 모드 확장 필요). Follow-ups: (a) auto-detect `Tab.mode='source'` from `notes.source_type='pdf'` on tab open, (b) Range/206 support for large PDFs, (c) ASCII-only fallback test for Content-Disposition, (d) E2E smoke 실행. |
| `2026-04-23-app-shell-phase-3-tabs.md`                 | ✅ 2026-04-24 (Plan 3-A scope)     | App Shell Phase 3-A — Tab bar chrome. tabs-store extensions (reorder/togglePin/promoteFromPreview/addOrReplacePreview/closeOthers/closeRight + persisted closedStack(cap 10)/restoreClosed, closeOthers/closeRight evict→closedStack), `lib/tab-factory` (i18n-agnostic newTab + genTabId), `useTabNavigate` (router-only navigator; split from useUrlTabSync to prevent double-mount reconciliation side-effects), use-url-tab-sync 마이그레이션(single-click stacking → `addOrReplacePreview`), `useTabKeyboard` (⌘W/⌘1-9/⌘←→/⌘⌥←→, input·contenteditable focus 가드로 cursor-nav 충돌 회피), `mod+T`/`mod+shift+T` shell-providers wiring, `useTabPreviewPromotion(noteId)` + `NoteEditor.onFirstEdit` one-shot(printable·backspace·delete·enter 한정), `NoteEditorClient` 클라이언트 island, `TabItem`/`TabBar`(@dnd-kit 4px activation)/`TabOverflowMenu`/`TabContextMenuItems`(pin/duplicate/close/closeOthers/closeRight/copyLink) + shadcn `ContextMenu` 래핑, `TabShell`로 `PlaceholderTabShell` 교체, `appShell.tabs.{bar,item,menu}` i18n(ko/en parity, 33 keys). web 31 files/195 tests · tsc clean · ESLint clean · Next build OK. `tests/e2e/tab-system.spec.ts` 7 scenarios (실행 deferred, `role="tab"` 셀렉터 사용). Plan 3-B(별도)로 이관: TabModeRouter + plate/reading/source/data viewer 통합, `/api/sources/:id/file` / `/api/notes/:id/data` 신설, 마우스 편집(paste/slash/drag-drop) → onFirstEdit 확장, i18n locale-switch 고착 해소(Tab.titleKey + render-time resolve). |
| `2026-04-09-plan-3-ingest-pipeline.md`                 | ✅ 2026-04-20, merge `c859a29` + Office/HWP follow-up 2026-04-28 (`feat/plan-3-office-hwp-followup`)   | `/api/ingest` (bodyLimit + MIME 허용리스트 + `canWrite`) + MinIO + Temporal 스캐폴드. 8 activities (PDF/STT/Image/YouTube/Web/Enhance/Note/Quarantine). `generate_multimodal` 확장, quarantine. **Office/HWP follow-up (2026-04-28)**: `parse_office` (markitdown for OOXML docx/pptx/xlsx/xls + unoserver→pymupdf for legacy doc/ppt) + `parse_hwp` (unoserver+H2Orestart→opendataloader-pdf) + IngestWorkflow MIME 분기 + temporal_main 등록 + Dockerfile (LibreOffice + python3-uno + unoserver 3.5 + H2Orestart 0.7) + start-worker.sh entrypoint(unoserver daemon). 18 신규 pytest pass (4 office + 4 hwp + 10 dispatch parametrize). 잔여 follow-up: scan PDF OCR (audit Tier 1 #5), streaming upload. ARM64 빌드 + dev 서버 라이브 검증은 별도(이미지 크기 +500MB).                                    |
| `2026-04-21-plan-3b-batch-embeddings.md`               | ✅ 2026-04-22, merge `8aadf14` + prod-gates 2026-04-25 | Infrastructure 완비 — `packages/llm` batch surface (4 provider methods + `embed_many()` helper), migration 0009 `embedding_batches`, ADR-008 `BatchEmbedWorkflow` + 3 activities, 이원 feature flag (`BATCH_EMBED_COMPILER_ENABLED` / `BATCH_EMBED_LIBRARIAN_ENABLED` 둘 다 기본 OFF). Compiler 루프 밖 batch 리팩터 완료. Librarian은 호환 교체 + TODO(Phase 2: `_merge_duplicates` 일괄 embed 리팩터 필요). Research는 non-goal. **Prod 활성화 gates (2026-04-25)**: `worker/lib/batch_timeouts.py` (플래그 ON 시 compile_note/run_librarian start_to_close 를 24h+10m 로 확장, import-time 계산), `worker/lib/batch_metrics.py` 구조화 로그 이벤트 (`batch_embed.{submit,poll_done,fetch,fallback}`), caller-side `BATCH_EMBED_MAX_ITEMS` 청킹 (`_chunk_inputs` + `_align_from_chunks`), M-5 `run_id[:8]` 콜리전 fix, `scripts/purge_embedding_jsonl.py` (JSONL TTL 수동/cron), `docs/contributing/ops.md` 런북. worker 105→160 / llm 74→77 pytest. Phase 2 잔여: `_merge_duplicates` 배치화, M-7 streaming `upload_jsonl`, Prometheus/OTEL 배선. |
| `2026-04-09-plan-4-agent-core.md`                      | ✅ 2026-04-21, merge `7947f9c`   | Phase A (Compiler, merge `3a9ef42`) + Phase B: `/internal/notes/hybrid-search` (pgvector + BM25 + RRF, migration 0006), Librarian 보조 엔드포인트, row-count `project_semaphore_slots`, Research/Librarian `runtime.Agent`, Librarian Temporal Schedule, worker `--profile worker`. 95+29 pytest. Follow-up: Task 8 E2E 스모크, `notes.embedding` 백필. |
| `2026-04-20-plan-9a-web-foundation-and-landing.md`     | ✅ 2026-04-20, HEAD `415d668`    | 테마(4팔레트) + i18n(next-intl, ko/en, ESLint no-literal-string, parity CI) + 랜딩 10섹션 포트 SSG + sitemap/robots + Playwright smoke. Lighthouse는 프로덕션 배포 시 수동. 본 Plan 후 모든 user-facing 문자열은 i18n 키 강제.                                                                                         |
| `2026-04-09-plan-9b-billing-engine.md`                 | 🔴 BLOCKED (사업자등록)          | PAYG 크레딧 엔진, Toss 연동, 결제 UI, 환불, Export(GDPR), 블로그, 법적 문서 본문. Plan 9a의 Pricing 섹션 숫자를 API로 교체.                                                                                                                                                                                         |
| `2026-04-22-ingest-source-expansion.md`                | ✅ 2026-04-22, merge `419c736`   | One-shot Google Drive + Notion ZIP import. `user_integrations` (AES-256-GCM tokens) + `import_jobs` tables (migration 0012). `/api/integrations/google/*` OAuth + `/api/import/drive` / `/api/import/notion` / job listing + SSE + cancel. Hybrid worker: `ImportWorkflow` drives `resolve_target` → `unzip_notion_export` OR `discover_drive_tree` → `materialize_page_tree` → per-node fan-out → `finalize_import_job`. Notion .md → Plate direct; binaries via child `IngestWorkflow`. `/w/[slug]/import` UI (Drive tab: file-id input MVP; Notion tab: ZIP upload + progress). ko + en i18n. Feature flag `FEATURE_IMPORT_ENABLED`. E2E scaffolded (Notion path). api 65+5 · worker 133+7 pytest.  Follow-ups: Google Picker SDK, retry signal, folder hierarchy in notes schema. |

## Phase 1 follow-ups

| Plan                                                 | Status | Summary                                                                                                                                                                                                                                                                      |
| ---------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `2026-04-22-onboarding-implementation.md`            | ✅ 2026-04-22, merge `dfaa28d` | `/{locale}/onboarding` 2-모드 라우트 (워크스페이스 생성 + 초대 수락). Backend prereq: `GET /api/invites/:token`, 예약 slug + 409 매핑, invite email URL 교정, `/test-seed` onboarding 모드 2종. Frontend: 서버 가드 + `CreateWorkspaceForm` + `AcceptInviteCard` + slug/return-to 헬퍼 + signup→verify→login→onboarding 토큰 승계. Playwright 4 specs. Spec: `2026-04-22-onboarding-and-first-run-design.md`. |
| `2026-04-22-agent-runtime-v2a-core-tool-loop.md`     | ✅ 2026-04-23, merge `93b6697` (PR #1) | Agent Runtime v2 · Sub-A Core Tool-Use Loop. `packages/llm` tool-calling surface (`tool_types`/`errors`/base/Gemini `generate_with_tools` + `tool_result_to_message`, Ollama stub raises `ToolCallingNotSupported`). `apps/worker/src/runtime/tool_loop.py` ToolLoopExecutor + hard/soft guards + termination paths (model_stopped/structured_submitted/max_turns/…/provider_error/cancelled). 6 builtin tools (`list_project_topics`/`search_concepts`/`search_notes`/`read_note`/`fetch_url` SSRF-hardened/`emit_structured_output`) + `ToolContextRegistry` + `runtime.loop_runner.run_with_tools` helper + `ToolDemoAgent` 4 presets. `/api/internal/projects/:id/topics` Hono route. Docs: antipatterns §12 + api-contract Internal table + context-budget §7 tool path budgets. Umbrella A 완료 마킹. Tests: llm 27 · worker 102 (runtime + tools_builtin + agents + security) · integration 4 skipped (GEMINI_API_KEY_CI gated). |
| `2026-04-23-react-email-adoption.md`                 | ✅ 2026-04-23, merge `791fc70` | `packages/emails` workspace + react-email v6 (Html/Container/Button primitives from `react-email`, `@react-email/ui` peer) + Resend React payload (`react:` over raw HTML). Templates: `Layout` (header/footer/RTL-safe), `Button`, `InviteEmail`. `apps/api/src/lib/email.ts` swapped from inline strings. Vitest 14 unit tests. |
| `2026-04-22-deep-research-phase-a-llm-wrapper.md`    | ✅ 2026-04-23, merge `8910563` (PR #4) | Deep Research integration · Phase A. `packages/llm` Interactions API wrapper. `interactions.py` dataclasses (`InteractionHandle`/`InteractionState`/`InteractionEvent` + `InteractionStatus`/`InteractionEventKind` Literal 별칭). `LLMProvider` base에 4개 async hook (`start_interaction`/`get_interaction`/`stream_interaction`/`cancel_interaction`) 기본 `NotImplementedError`. `GeminiProvider`가 `client.aio.interactions.create/get(stream=True)/cancel` 로 위임 구현 (스트리밍은 `get(stream=True, last_event_id=...)` 경유 — SDK에는 별도 `.stream()` 없음). `OllamaProvider`는 상속된 기본값 유지 + 회귀 테스트 4개로 핀. Spec Open Question #1 (SDK 버전 확인) 해결: `google-genai` 1.73.1에서 `client.aio.interactions.*` 노출 확인. 121/121 pytest. 다음: Phase B (DB + Temporal workflow + activities). |
| `2026-04-23-deep-research-phase-c-api-routes.md`     | ✅ 2026-04-23, merge `a838524` (PR #6 + #7 + #8 + #9) | Deep Research integration · Phase C. `apps/api/src/routes/research.ts` (8 public endpoints: POST/GET/GET/POST/PATCH/POST/POST/GET stream + `loadRunForUser` helper) mounted at `/api/research` behind `FEATURE_DEEP_RESEARCH` flag. `/api/internal/notes` 확장 (idempotencyKey + plateValue + noteId 추가, legacy ingest-expansion shape 유지). `/api/internal/research/image-bytes` 신설 (payload->>url 매칭). `packages/shared/src/research-types.ts` (Zod schemas + response types + SSE event union). Cross-workspace 404 (api-contract). SSE는 `import.ts` 2s poll 패턴 복제. Temporal mock으로 hermetic. Tests: api 116/116 (research 21 + internal-research 7 + 기존 88), shared 16/16. Follow-ups (FU-1~FU-13): UUID regex tighten, `ResearchRunDetail` 응답 타입 주석, turns+artifacts `Promise.all`, `taskQueue()` 중복, `research_runs.updatedAt.$onUpdate` (Phase B 파일), POST/turns TOCTOU seq race, `loadRunForUser` GET/runs/:id 재사용, 409 shape shared 타입, "rejects when status is researching/completed/failed/cancelled" 테스트가 researching만 exercise, approve insert+update 트랜잭션 부재, approve 이중클릭 guard, `{alreadyTerminal}` shared 타입, test file describe 블록 chronological 정렬. 다음: Phase D (apps/web). |
| `chore/tech-debt-sprint-1` (no plan doc)            | ✅ 2026-04-27 PR #53 MERGED (main `59bb8f3`) | Tech debt sprint — Phase 5 follow-ups (a/c/f) + Plan 7 Tier-S. `import_done` system notification + `/api/internal/notifications` publish surface, command palette → locale layout (외부 라우트에서도 ⌘K 활성화), `/api/notifications` opaque cursor pagination, `extract-ws-slug` 분리, `purge_canvas_outputs.py` 30일 cleanup cron, `find_orphan_keys` 청크 DB 조회 최적화, Grafana `canvas-outputs` 대시보드. api +271 tests, observability README 추가. |
| `2026-04-22-deep-research-phase-b-workflow.md`       | ✅ 2026-04-23, merge `3b03154` (PR #3) | Deep Research integration · Phase B. `packages/db` migration 0013 (research_runs + turns + artifacts + user_preferences.byok_api_key_encrypted, 6 enums). `apps/worker/activities/deep_research/` 4 activities: `create_plan` (collaborative_planning 폴 루프), `iterate_plan` (previous_interaction_id chaining), `execute_research` (stream_interaction + heartbeat + on_event 콜백), `persist_report` (MinIO 업로드 + markdown_to_plate + research-meta 블록 + `/internal/notes` idempotent POST). Helpers: cost estimator (§7.4 공식), key resolver (BYOK AES-256-GCM decrypt vs managed env), markdown-to-Plate wrapper (notion_activities.md_to_plate 재사용). `DeepResearchWorkflow`: 3 signals (user_feedback/approve_plan/cancel) + 24h abandon via wait_condition + 이중 feature flag gate. `temporal_main` 등록은 FEATURE_DEEP_RESEARCH 뒤. 29/29 pytest green (activity + helper). Workflow time-skipping 테스트 3개는 로컬 flake로 skipped — Phase C 통합에서 커버. Open: `/internal/notes` + `/internal/research/:runId/artifacts` + `/internal/research/image-bytes` endpoint (Phase C), BYOK 등록 UI (Phase D). |
| `2026-04-27-live-ingest-visualization.md`            | ✅ 2026-04-28, branch `feat/live-ingest-visualization` (미머지) | Live Ingest Visualization — Redis pub/sub + SSE + spotlight/dock UI for real-time ingest progress. **Backend (Phase 1)**: `packages/shared/ingest-events.ts` (zod discriminated union, 9 event kinds), `apps/api/lib/redis.ts` (ioredis singleton), `apps/worker/lib/ingest_events.py` (publish_safe wrapper — Redis downtime never breaks ingest), `pdf_activity` page-loop with `--extract-images=true` + per-page `unit_started`/`figure_extracted`/`unit_parsed` emission via MinIO upload, non-PDF activities emit minimal stage_changed events, `IngestWorkflow` propagates `workflow_id` + `started_at_ms` into every activity input + `emit_started` activity, `GET /api/ingest/stream/:wfid` Hono streamSSE handler (LIST replay → SUBSCRIBE live + Last-Event-ID dedup + 30s keepalive + close-on-completed/failed). **UI (Phase 2)**: `messages/{ko,en}/ingest.json` (29 keys parity), zustand `ingest-store` (idempotent applyEvent on seq + persist trim), `useIngestStream` hook (EventSource + Zod parse), `IngestProgressView` 3-mode shared container, `IngestSpotlight` (auto-collapse on first figure / first unit / 7s) + `IngestDock` (12 cards + overflow + per-card SSE subscriber), `IngestOverlays` shell mount gated by `NEXT_PUBLIC_FEATURE_LIVE_INGEST`, Tab.kind/mode `"ingest"` + `IngestViewer` + dock open-in-tab, `useIngestCompletionRedirect` (5s push to /notes/:id, cancellable), `GET /api/ingest/figures/:wfid/:filename` proxy. **No DB migration**. shared 5/5 · api 6/6 (redis + stream) · worker 12/12 (ingest_events + pdf_activity + event_emission + workflows) · web 35/35 (store + progress-view + spotlight + dock + redirect-hook) · i18n parity 29 keys. **Manual smoke + Playwright E2E deferred** — needs full live stack with Java JAR + worker + auth + PDF fixture. **Out-of-scope (별도 plan)**: rate-limit/visualize-lock Redis 마이그레이션 (TODO 재사용), B spec enrichment 위젯, E spec multi-file dispatch verification, mobile spotlight, /api/ingest/upload UI wiring (no upload UI exists in apps/web today). |

### Deep Research integration (Spec: `2026-04-22-deep-research-integration-design.md`)

- ✅ Phase A — `packages/llm` Interactions wrapper (2026-04-23)
- ✅ Phase B — DB + Temporal workflow (2026-04-23, merge `3b03154`)
- ✅ Phase C — apps/api routes + SSE (2026-04-23, merge `a838524`)
- ✅ Phase D — apps/web `/research` UI + research-meta block (2026-04-25, branch `feat/deep-research-phase-d`). 23 tasks · feature-flagged routes (`(shell)/research`, `(shell)/research/[runId]`) + flag-gated sidebar icon · `api-client-research` (8 endpoints) + `useResearchStream` SSE hook · 4 client components (NewResearchDialog, ResearchHub, ResearchPlanReview, ResearchProgress, ResearchRunView orchestrator) · Plate v49 `research-meta` void block + Yjs roundtrip pin · `research.json` i18n (72 keys ko/en parity) · web 49 files/278 tests · tsc + lint clean · `tests/e2e/research-smoke.spec.ts` (skipped without `FEATURE_DEEP_RESEARCH`).
- ✅ Phase E (features) — BYOK 등록 UI + `/settings/ai` + en native review + research-smoke E2E activation (2026-04-26, branch `feat/deep-research-phase-e`). 11 tasks · API GET/PUT/DELETE `/me/byok-key` (encryptToken/decryptToken byte-compat with worker, decrypt-failure recovery returns `{registered:false}`) · `api-client-byok-key` typed wrapper with `ByokKeyApiError.code` for i18n branching · `ByokKeyCard` Client Component (3 states: empty/registered/editing) + delete-confirm Dialog · `/[locale]/app/settings/ai` Server Component (auth guard, NOT gated by FEATURE_DEEP_RESEARCH) · `messages/{ko,en}/settings.json` (27 keys parity, native EN pass for both `research.json` 73 keys and `settings.json`) · `playwright.config.ts` injects `FEATURE_DEEP_RESEARCH=true` into web webServer · 13 byok-key API tests + 5 ByokKeyCard tests + 6 api-client tests + `tests/e2e/settings-ai.spec.ts` smoke. api 13 added byok tests · web 17 namespaces parity green · prod flag flip kept out of PR (env-only).
- 🟡 Phase E prod release — manual env flip (`FEATURE_DEEP_RESEARCH=true`) pending after staging verification. No code work; post-merge env update on staging then prod, followed by a small docs commit recording the deploy date + sha.

## Phase 2 — Scale (Plan 4 후, 병렬 가능)

| Plan                                           | Summary                                                                                          |
| ---------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| `2026-04-09-plan-5-knowledge-graph.md`         | ✅ Phase 1 (PR #43) + Phase 2 (PR #48 + review fixes PR #49) MERGED. LightRAG 동기화, Cytoscape 5뷰 (Graph/Mindmap/Cards/Board/Timeline) + Backlinks, Visualization Agent (NL→ViewSpec SSE). Phase 1 wiki_links + Backlinks panel + cytoscape graph view, Phase 2 4-view expansion + VisualizeWorkflow + AI dialog + 1-5 keyboard shortcuts + E2E. Phase 2 review fixes: heartbeat IPayloads decode, MindmapView tap rebind, visualize lock release on cancel, concept created_at + stable fetchNodesByIds order, heartbeat event accumulation. |
| `2026-04-09-plan-6-learning-system.md`         | ✅ 2026-04-26 PR #50 MERGED (`d458591`). 6 commits. **Task 1**: flashcard CRUD + SM-2 review routes + deck_name migration. **Task 2**: Socratic Agent (generate questions + evaluate answers via Temporal). **Task 3+4**: Tool Template engine + 9 built-in templates (`packages/templates`). **Task 5+6**: flashcard review UI + understanding scores dashboard. **Review fix**: api+web findings addressed. Cards 뷰 통합은 Plan 5 Phase 2 BoardView로 충족. |
| `2026-04-09-plan-7-canvas-sandbox.md`          | 브라우저 샌드박스 (Pyodide + iframe, ADR-006), Code Agent. **Phase 1 (web runtime + Tab Mode Router) ✅ 2026-04-25, branch `feat/plan-7-canvas-phase-1`** — 17 tasks · DB `source_type='canvas'` + `canvasLanguage` enum + CHECK invariant (migration 0020/0021) · `packages/shared` `canvasLanguageSchema` + `MAX_CANVAS_SOURCE_BYTES` + `patchCanvasSchema` · `apps/api` POST/GET extensions + dedicated `PATCH /api/notes/:id/canvas` (canRead → 404 hide, sourceType≠canvas → 409, canWrite → 403) · `apps/web` PyodideRunner (10s timeout `Promise.race`) + CanvasFrame (`sandbox="allow-scripts"` only, Blob URL revoke, 64KB encoded-byte guard) + `useCanvasMessages` (origin=`null` + source identity) + `sandbox-html-template` (esm.sh react@19 pinned) + `pyodide-loader` (lazy CDN, version `0.27.0` pinned) · CanvasViewer Tab Mode adapter (debounced 1.5s save) + tab-mode-router `canvas` case + `modeFromSourceType` helper · `/canvas/demo` standalone playground (auth-gated) · sidebar `NewCanvasButton` · canvas i18n namespace (32 keys ko/en parity) · CSP headers (esm.sh + jsdelivr/pyodide allowlist, `unsafe-eval` for WASM, `frame-src blob:`) · Playwright `canvas.spec.ts` (6 tests; deferred run, env-dependent) · `scripts/canvas-regression-guard.sh` (allow-same-origin / postMessage `*` / pyodide latest) + `pnpm canvas:guard`. db 7 · shared 26 · api 249 · web 301 = **583 tests pass** · build clean · i18n parity clean. **Phase 2 (Code Agent + /api/code/run + Monaco + matplotlib MinIO + E2E hold-backs) ✅ 2026-04-26, branch `feat/plan-7-canvas-phase-2`** — 24 tasks · DB `code_runs` + `code_turns` + `code_run_status` enum + `canvas_outputs` + `canvas_outputs_note_hash_unique` UNIQUE (migration 0022) · `packages/shared` `canvas-outputs.ts` + `code-agent.ts` schemas (run/feedback/SSE event union) · `apps/worker` `agents/code/` (CodeAgent · generate/analyze tools · brace-escape prompt template) + `workflows/code_agent_workflow.py` (signal-driven loop, max 3 fix turns, 30min idle, RetryPolicy max=2, 1h exec timeout) + 2 activities (`generate_code` / `analyze_feedback`) · `apps/api` `routes/code.ts` (run/stream/feedback + 8-state allow-list) + `routes/canvas.ts` (output multipart + outputs list + outputs/:id/file) + `routes/internal-code.ts` (turns idempotent + status PATCH) · `apps/web` `CodeAgentPanel` + `CodeAgentLog` + `CanvasOutputsGallery` + Monaco code editor (lazy + dynamic import) · PyodideRunner matplotlib hook (`MPLBACKEND=Agg` + `plt.get_fignums()` → base64 PNG `figures[]`) · canvas i18n delta (parity ko/en) · feature-flag `FEATURE_CODE_AGENT` (off → 404) · `/api/canvas/from-template` 501 stub (Plan 6 dep) · Playwright `code-agent.spec.ts` (5 hold-back + 2 active). Plan 6 templates dependency holds `from-template` as 501 stub until templates land. Tier-S follow-ups: `scripts/purge_canvas_outputs.py` (30-day cleanup) + Grafana `canvas-outputs` dashboard panels. |
| `2026-04-09-plan-8-remaining-agents.md`        | ✅ 2026-04-27 PR #55 `feat/plan-8-remaining-agents` (9 commits, HEAD `664cf32`). **5 agents**: Synthesis (merge notes → new wiki note via LLM), Curator (orphan/duplicate/contradiction detection → `suggestions` table), Connector (cross-project pgvector similarity → `connector_link` suggestions), Temporal/Staleness (stale wiki detection + LLM scoring → `stale_alerts`), Narrator (2-speaker podcast TTS + MinIO → `audio_files`). **DB**: `suggestions` + `stale_alerts` + `audio_files` tables (migration 0032). Deep Research Agent (Task 7) skipped — fully implemented in prior Phases A-E. **65/65 pytest**. Code-review fixes (2 rounds): `make_interval()` SQL · `asyncio.get_running_loop()` · workspaceId ownership guard on `/internal/suggestions` · StalenessWorkflow semaphore · `scoreThreshold` surfaced · narrator default alignment · `asyncio.gather` parallelization (staleness LLM calls, curator suggestion POSTs, synthesis note fetches) · Zod UUID validation in connector/curator routes · sliding-window contradiction pairs · robust JSON fence stripping. |

## Phase 3 — Add-ons

| Plan                                                 | Summary                                                                                                                                 |
| ---------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| `2026-04-15-plan-10-document-skills.md`              | ⚠️ **SUPERSEDED** by `2026-04-27-plan-multi-format-synthesis-export.md` (server-side Tectonic MSA 가정 + ADR-006 이전 설계, 미실행). Plan 10 + 동명 spec 헤더에 supersede 명시.                                                                                            |
| `2026-04-27-plan-multi-format-synthesis-export.md`   | 🟡 plan only, branch `feat/plan-synthesis-export` (worktree `.worktrees/plan-synthesis-export`, `1f616ba`, 미실행). 31 task / 6 phase / 4941줄. LaTeX/DOCX/PDF/MD synthesis. Tectonic MSA Pro tier. Migration 0032 (Plan 8 0032 와 충돌 — 머지 시 재번호 필요). 플래그 2종 (`FEATURE_SYNTHESIS`, `FEATURE_TECTONIC_COMPILE`) 기본 OFF. spec 은 main `347a040` 에 머지됨. Plan 10 supersede.                                                                                            |
| `2026-04-27-live-ingest-visualization.md`            | 🟡 진행 중, branch `feat/live-ingest-visualization` (worktree `.worktrees/live-ingest-viz`, 4 commits 미머지). spec/plan 18 task 중 백엔드 일부: `IngestEvent` Zod discriminated union (`d378637`) + ioredis singleton + `REDIS_URL` doc (`339c3c5`) + redis-backed emitter + `publish_safe` (`5480bf6`) + `parse_pdf` 페이지 루프 figure 추출 + IngestEvent 발행 (`4b3755d`). 잔여: web SSE client + `note_ingest_artifacts` 테이블 + 진행 UI (carousel + progress bar).                                                                                            |
| `2026-04-27-literature-search-import.md`             | ✅ 2026-04-28 PR #57 MERGED (main `85067f4`) + PR #59 review fixes (main `1a8f4c1`). 12 tasks / 12 commits. **DB**: migration 0033 (plan said 0029; stale — main was on 0031, plan-8 reserves 0032). `notes.doi` text + `notes_workspace_doi_idx` partial unique (workspace_id, doi WHERE doi IS NOT NULL) + `import_source += 'literature_search'` + `source_type += 'paper'`. **API**: `lib/literature-search.ts` federation (arXiv Atom XML + Semantic Scholar graph/v1 + Crossref fallback + Unpaywall enrichment, AbortSignal.timeout 5–10s, contact email env-driven). `routes/literature.ts` 3 endpoints — GET `/search` (workspace-membership gate via resolveRole + 60req/60s rate-limit per workspace + sources query parsed against allow-list + paginated upstream so page N+1 keeps the tail of N) · POST `/import` (canWrite gate · 50-id ceiling · ≤3 concurrent imports per workspace · DOI dedupe pre-check · `importJobs` insert + LitImportWorkflow startWorkflow on `taskQueue()`) · GET `/import/:jobId` (owner-only). `routes/internal.ts` extension — `internalNoteCreateSchema += doi` + `'paper'` in sourceType enum + `internalNotePatchSchema += 'paper'` + GET `/internal/notes?doi=&workspaceId=` cooperative dedupe lookup + `/internal/literature/{search,import}` agent-tool surface (workspace_mismatch guard on projectId.workspaceId). **Worker**: `activities/lit_import_activities.py` 4 activities — `fetch_paper_metadata` (DOI/arXiv → metadata + OA URL via SS-then-Unpaywall) · `lit_dedupe_check` (workspace-scoped DOI dedupe; arxiv-only ids always fresh) · `create_metadata_note` (paywalled fallback, paper_meta Plate block) · `fetch_and_upload_oa_pdf` (50MiB ceiling + content-type sniff + SSRF guard re-checked after redirect, async upload via `asyncio.to_thread(upload_object, …)`). `workflows/lit_import_workflow.py` 5-phase orchestration (resolve_target → fetch → dedupe → fan-out → finalize, child IngestWorkflow per OA paper, graceful degradation to metadata-only on PDF failure). `temporal_main.py` registration appended at END of imports + workflows + activities lists (additive diff to keep merge surface small against parallel `feat/content-aware-enrichment` session). `tools_builtin/literature_{search,import}.py` agent tools wired into `BUILTIN_TOOLS` (now 9). **Web**: TabKind += `'lit_search'`, TabMode += `'lit-search'`. `stores/lit-search-store.ts` (transient per-tab payload, in-memory only — page refresh re-issues search). `components/chat/lit-result-card.tsx` (top-5 chat bubble + OA/paywall badge + "Open full results" → addTab + lit-search-store). `components/tab-shell/viewers/lit-search-viewer.tsx` (full-page table with checkbox-row select + re-search bar + import bar + project picker). `tab-mode-router.tsx` `case "lit-search"`. Pre-existing exhaustiveness gap fixed on the way: `tab-url.ts` + `use-url-tab-sync.ts` switches were missing the `ingest` case (introduced by live-ingest-viz merge); both `ingest` and `lit_search` added together so TS passes. **i18n**: `messages/{ko,en}/literature.json` 30 keys parity (next-intl `{count}` syntax — plan used i18next `{{count}}`, converted on the way). **Tests**: worker pytest 8/8 (helpers + activities via `temporalio.testing.ActivityEnvironment`). API tests 14 specs (literature-search + literature-import) — TS clean, DB-bound run deferred to CI (local PG port 5432 ACL conflict). web tsc clean, ESLint clean (one decorative emoji 📄 wrapped in `aria-hidden` + eslint-disable comment). i18n parity 30/30. **Plan stale fixes recorded in commit messages**: migration number (0029→0033), `worker.lib.s3.upload_bytes` → `worker.lib.s3_client.upload_object` (sync, threaded), `taskQueue` is a function not a string, `openTab` doesn't exist (use `addTab` + sibling store for meta), `vi.spyOn` on module via `import * as`, `{{var}}` → `{var}`, Tab type has no `meta` field. **Out-of-scope (Tier-1 follow-ups)**: title-based dedupe for arxiv-only papers, project picker dropdown (currently free-text uuid), workspace-wide job visibility, agent-tool E2E run, prod E2E with real Postgres + Temporal + MinIO. |
| `2026-04-27-content-aware-enrichment.md`             | ✅ 2026-04-28, branch `feat/plan-content-enrichment` (worktree `.worktrees/plan-content-enrichment`, 미머지). 8 task / 9 commits (8 plan + 1 review-fix). **DB**: migration 0034 (plan said 0029 — stale; main was on 0031 + plan-8 0032 + literature-search 0033). `note_enrichments` (id, note_id FK cascade + UNIQUE INDEX, workspace_id FK, content_type, status default 'pending', artifact jsonb, provider, skip_reasons text[], error, timestamps). Journal idx 33 (after literature-search 0033). **Worker**: `lib/enrichment_artifact.py` pydantic models (7 content types share `EnrichmentArtifact` envelope: outline · figures · tables · sections · slides · chapter_tree · symbol_tree · pivot_suggestions · translation). `activities/detect_content_type_activity.py` heuristic 7-way classifier (mime-only for slide/code/image; first-3-pages signals for paper; first-10-pages for book; table-fraction for table) + Gemini Flash Lite fallback only when conflicting signals (confidence < 0.7). `activities/enrich_document_activity.py` per-type: document/paper Gemini multimodal preferred → text-only fallback (Ollama always text), slide cards constructed client-side without LLM, code = Python AST symbols, book/table/image = Gemini per-prompt. Translation skipped on Ollama. Pydantic `model_validate` with raw-dict fallback so partial artifacts still reach DB. `_TRANSLATION_HEADER + src` concatenation (str.format would crash on `{...}` braces in JSON-rich abstracts). `_build_figure_items` reconstructs object_keys from live-ingest-visualization's `uploads/{user}/figures/{wf}/p{p}-f{f}.png` formula instead of re-uploading (avoids duplicate bytes + race-prone parent_note keys). `activities/store_enrichment_activity.py` thin wrapper around `post_internal` (separated from enrich so retries hit cheap HTTP only). **API**: `routes/internal.ts` `POST /notes/:noteId/enrichment` (zValidator + workspace mismatch check via `notes.workspaceId` lookup + `ON CONFLICT (note_id) DO UPDATE` upsert) + `GET /notes/:noteId/enrichment?workspaceId=` (mandatory query + workspace-mismatch defence-in-depth). **Workflow**: `IngestWorkflow.IngestInput += workspace_id`, detect→enrich splice runs after parse / before create_source_note (memory-only result), store splice runs after note_id is known. Both wrapped in `except (ActivityError, ApplicationError)` — narrow catch keeps dev signal, feature flag protects prod. `_activity_input` helper threads `workflow_id` + `started_at_ms` into enrichment activity input (matches live-ingest-viz convention). Flag-gated under `FEATURE_CONTENT_ENRICHMENT` (default OFF) at `temporal_main.build_worker_config` — additive diff at end keeps merge surface small. **Tests**: worker pytest 30/30 (artifact schema 6 + detect 8 + enrich 8 + workflow 4 + temporal_main 4). DB vitest gap (vitest 4.1.5 broken on this Windows worktree — same byte-identical files work on main). API tests vitest gap (same root cause). API tsc clean for internal.ts. Postgres-bound migration apply not run locally (Postgres not running). **Code review applied**: B1 (pdf_activity reverted live-ingest-viz work — fixed via rebase + restore main's version with single +pages return key), B2 (drizzle journal hand-edited — regenerated via `drizzle-kit generate` post-rebase), B3 (figure object_keys mismatch — replaced base64 upload with key reconstruction), I1 (no unique on note_id — added uniqueIndex), I2 (GET no workspace check — mandatory ?workspaceId), I4 (`except Exception` too broad — narrowed), M2 (TRANSLATION_PROMPT.format crash on stray braces — concat header + regression test). **Out-of-scope (follow-ups)**: contentType pgEnum, M1 LLM confidence re-derivation, full E2E with real Temporal + MinIO + Postgres, vitest worktree-resolution bug investigation. **Merge dependencies**: rebased onto literature-search merge (main `85067f4`), journal idx 33 / migration 0034 chains correctly through 0033_literature_search. |
| `2026-04-20-plan-11a-chat-scope-foundation.md`       | ✅ 2026-04-27 PR #54 MERGED (main `beb3b3f`). 14 tasks · DB `conversations` + `conversation_messages` + `pinned_answers` (migration 0029, `scope_type` / `rag_mode` / `conversation_message_role` enums; role enum renamed to avoid collision with App Shell Phase 4 `message_role`) · `packages/shared/chat.ts` Zod schemas (CreateConversation/Patch/Pin/SendMessage/AddChip + ChipType drift test) · `apps/api/routes/chat.ts` (POST/GET/PATCH/LIST conversation + chip add/remove + SSE message + pin + pin/confirm) + `lib/chat-scope.ts` validateScope + `lib/pin-permissions.ts` computePinDelta + `lib/cost.ts` USD→KRW · `apps/api/routes/search.ts` `/scope-targets` chip combobox backing · `apps/web/hooks/useScopeContext.ts` (page/project/workspace from URL) · `apps/web/components/chat-scope/` (Chip · ChipRow · AddChipCombobox · RagModeToggle · PinButton · PinPermissionModal · CostBadge · ChatInput · ChatPanel) · `(shell)/chat-scope` + `(shell)/p/[projectId]/chat-scope` 페이지 · `messages/{ko,en}/chat-scope.json` 22 keys parity. db 25/25 · shared 80/80 · api 519/519 · web 532/532. SSE 메시지는 placeholder 응답("(11A placeholder reply)") — 실제 LLM 통합은 Plan 4 worker 경유로 Plan 11B에서. activity_events 미사용 → `pinned_answers.reason` 컬럼으로 audit 대체. E2E 3 specs는 Onboarding 관례대로 deferred. **Plan 11B/11C는 후속 brainstorm-plan 사이클**. |
